import 'package:flutter/material.dart';

class TSizes {
  TSizes._();

  static const appBarHeight = 60.0;
  static const defaultSpace = 20.0;
  static const spaceBtwInputFieds = 20.0;

  // const TextStyle titleStyle = TextStyle(fontSize: 25);
}
